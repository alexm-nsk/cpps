code builder class
separate the loader
embedded qsort
pragmas for built-ins
add mod and other functions
make separate real to string method in c++ backend
add wrapper for tuples in code generator

opts:
    find same literals
    find all nodes of specific types
    get all inputs

drawings:
    add interactivity like code highlighting on mouse over
    draw connections in multi nodes indicating value transfering
